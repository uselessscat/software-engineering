1. versionado y ciclos de vida  
    - Semantic versioning (mayor, menor, patch)  
    - Ciclos de release y políticas EOL  
    - LTS
2. compatibilidad entre versiones  
    - Python 2 vs 3  
    - Forward/backward compatibility  
    - PEP 563, PEP 585, transiciones
3. diferencias entre runtimes  
    - CPython, PyPy, Jython, IronPython  
    - Compiladores alternativos y VMs
4. herramientas para manejar versiones  
    - `pyenv`, `asdf`, `conda`, `virtualenv`, `venv`  
    - Detección de versión (`sys.version`, `platform.python_version()`)