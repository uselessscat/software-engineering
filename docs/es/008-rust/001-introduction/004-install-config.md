1. Gestores de runtime  
    - `rustup` como instalador oficial  
    - Canales `stable`, `beta`, `nightly`
2. Gestores de paquetes  
    - `cargo` preinstalado con `rustup`  
3. Configuración del entorno  
    - Windows: MSVC vs GNU  
    - macOS (brew/pkg)  
    - Linux (paquetes vs rustup)  
    - Off-line cache  
4. Instalación de toolchains  
    - `rustup target add`  
    - `rustup component add rustfmt clippy`  
    - Linkers para cross-compiling  
    - Mirrors internos en empresas