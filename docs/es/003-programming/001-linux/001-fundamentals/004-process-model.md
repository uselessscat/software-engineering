
# Procesos y ejecución

## Procesos en Unix

* PID, PPID
* Árbol de procesos
* Estados: running, sleeping, zombie

## Ciclo de vida de un proceso

* Creación con `fork()`
* Reemplazo con `exec()`
* Finalización y códigos de salida

## Espacios de memoria

* Userland vs kernel
* Páginas y layout del proceso (conceptual)

## Scheduler y planificación

* Time-slice
* Prioridades y nice/renice

## System calls esenciales

* `open`, `read`, `write`, `close`
* `fork`, `exec`, `wait`

## Scripts ejecutables

* Shebang
* Rutas y `PATH`
* Ejecución dentro de shells
