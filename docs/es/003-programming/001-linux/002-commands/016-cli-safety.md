# Buenas prácticas y seguridad

## Prevención de errores

* Evitar `rm -rf` en rutas no verificadas
* Probar comandos con `echo` antes de ejecutarlos

## Uso seguro

* Confirmaciones interactivas (`-i`)
* Validaciones de entrada
* Evitar expandir globbing accidentalmente
