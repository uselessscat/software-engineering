# Descarga y transferencia de datos

## Transferencia HTTP

* `curl`: solicitudes GET/POST, headers, subida de archivos
* `wget`: descargas recursivas, reintentos

## Transferencia remota

* `scp`: copia segura por SSH
* `rsync`: sincronización incremental, flags (`-avz`, `--delete`)
