1. ejecucion interactiva  
    - REPL de Node  
    - Consola del navegador  
    - Entornos sandbox (CodeSandbox, StackBlitz)  
2. scripts y archivos ejecutables  
    - `node archivo.js`  
    - `ts-node`, `tsx`  
    - Compilación con `tsc` + ejecución  
    - Uso de módulos ES (“type”: “module”)  
3. pipelines de ejecucion  
    - I/O estándar (`stdin`, `stdout`, `stderr`)  
    - Pipes y redirección  
    - Hot reload (`nodemon`, `ts-node-dev`)  
4. depuracion  
    - Flags de Node (`--inspect`, `--experimental-*`)  
    - Debuggers: Chrome DevTools, Node Inspector  
    - Sourcemaps y depuración en navegador  