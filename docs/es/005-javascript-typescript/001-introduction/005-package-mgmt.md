1. registros y repositorios  
    - npm Registry, repos privados (Artifactory, Verdaccio)  
2. manifest y lockfiles  
    - `package.json`, `package-lock.json`, `yarn.lock`, `pnpm-lock.yaml`  
3. resolucion de dependencias  
    - Determinismo, workspaces  
    - Resolución de conflictos, peer deps  
4. seguridad en la gestion de paquetes  
    - Auditorías (`npm audit`, `pnpm audit`)  
    - Verificación de integridad (shasum)  
5. scripts y hooks  
    - Scripts (`npm run`)  
    - Publicación de paquetes internos  
    - Hooks de build, `typesVersions`