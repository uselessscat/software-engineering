1. versionado y ciclos de vida  
    - Versionado semántico en TypeScript  
    - Ciclos LTS de Node.js  
2. compatibilidad entre versiones  
    - `target`, `lib` y downleveling  
    - Compatibilidad con navegadores y entornos legacy  
    - Polyfills (`core-js`)  
3. diferencias entre runtimes  
    - Node, Deno, Bun: divergencias y compatibilidad  
    - Hosts y especificación del Event Loop  
4. herramientas para manejar versiones  
    - `nvm`, `fnm`, `asdf`  
    - Detección de versión (`process.versions`, `navigator.userAgent`)  
    - Compatibilidad con distintos bundlers (`webpack`, `esbuild`, `vite`)
