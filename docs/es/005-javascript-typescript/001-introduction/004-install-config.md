1. gestores de runtime  
    - Instalador oficial de Node  
    - Deno y Bun como runtimes alternativos  
    - `nvm` / `fnm` / `asdf`  
2. gestores de paquetes  
    - `npm`, `yarn classic`, `yarn modern`, `pnpm`  
3. configuracion del entorno  
    - Verificación de PATH (`node`, `npm`, `tsc`)  
    - `npm init`, `pnpm init`, `yarn init`  
    - Inicialización de TypeScript (`tsc --init`)  
    - Estructura recomendada (`src`, `dist`, `test`)  
4. instalacion de toolchains  
    - Tooling de compilación para runtimes  
    - Contenedores y devcontainers (`Dockerfile`, `devcontainer.json`)  
    - Configuración multiplataforma (Win/macOS/Linux)